# Databricks notebook source
# MAGIC %md
# MAGIC # Query Mosaic AI Vector Search
# MAGIC
# MAGIC Example notebook that would query the index from Databricks using the Python SDK.

# COMMAND ----------

# Example only:
# from databricks.vector_search.client import VectorSearchClient
# client = VectorSearchClient()
# index = client.get_index(endpoint_name="document-rag-endpoint", index_name="main.document_rag.doc_index")
# results = index.similarity_search(query_text="What must be enabled before using a Delta source table?")

print("Vector search query notebook placeholder")
